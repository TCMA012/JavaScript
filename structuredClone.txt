The global structuredClone() method creates a deep clone of a given value using the structured clone algorithm.
Function properties aren’t supported.
To handle such complex cases we may take an existing implementation
_.cloneDeep(obj) from the JavaScript library lodash.